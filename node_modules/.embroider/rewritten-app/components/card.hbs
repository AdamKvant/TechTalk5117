<div class="p-6 max-w-xl bg-white rounded-xl flex items-center gap-x-4 shadow-2xl" {{on "click" this.flip}}>
    <div>
        <div class="text-xl font-medium text-black">{{@title}}</div>
        <p class="text-slate-500">{{@description}}</p>
        <!--this and helper is required to have "ember install ember-truth-helpers" !-->
        {{#if (and this.isFlipped @code1)}}
            <div class="p-6">
                <pre class="bg-sky-50"><code>{{@code1}}</code></pre>
                <br>
                {{{@code1}}}
                {{#if @code2}}
                    <br>
                    <pre class="bg-sky-50"><code>{{@code2}}</code></pre>
                    <br>
                {{{@code2}}}
                {{/if}}
                {{#if @code3}}
                    <br>
                    <pre class="bg-sky-50"><code>{{@code3}}</code></pre>
                    <br>
                {{{@code3}}}
                {{/if}}
            </div>
        {{/if}}
    </div>
</div>

{{yield}}